{
  "name": "teddy-mfe-jps",
  "private": true,
  "packageManager": "pnpm@10.15.1",
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "turbo": "^2.0.0",
    "vercel": "^47.1.4"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "preview": "turbo run preview"
  }
}